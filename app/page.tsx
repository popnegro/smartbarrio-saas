import Link from 'next/link';
import {ArrowUpRight,MapPin,Search,Store,Navigation} from 'lucide-react';
import {categories,commerces,zones} from '@/lib/data';
import {PublicHeader} from '@/components/PublicHeader';
import {PublicFooter} from '@/components/PublicFooter';
export default function Home(){return <div className="site-wrap"><PublicHeader/><main className="public-main"><section className="hero-card"><div className="hero-copy"><div className="eyebrow"><span className="dot green"></span>Marketplace local · Mendoza</div><h1>Encontrá lo que necesitás cerca tuyo.</h1><p>Comercios de barrio, productos y servicios de Las Heras y Maipú en una experiencia simple y navegable.</p><div className="hero-actions"><Link className="btn primary" href="/categoria/kioscos"><Store size={18}/> Explorar comercios</Link><a className="btn secondary" href="#explorar"><Search size={18}/> Buscar</a></div><div className="trust-row"><span><MapPin size={15}/> Las Heras</span><span><MapPin size={15}/> Maipú</span></div></div><div className="hero-visual green"><div className="visual-badge">SmartBarrio</div><div className="visual-initials">SB</div><div className="visual-caption"><MapPin size={15}/> Marketplace local de Mendoza</div></div></section><section id="explorar" className="market-section"><div className="section-head"><div><span className="kicker">DESCUBRIMIENTO</span><h2>Categorías</h2></div><span className="section-note">{Object.keys(categories).length} rubros disponibles</span></div><div className="market-grid">{Object.entries(categories).map(([slug,name])=><Link className="market-card" href={'/categoria/'+slug} key={slug}><div><span className="market-icon"><Store size={18}/></span><h3>{name}</h3><p>Explorá comercios, productos y servicios.</p></div><ArrowUpRight size={16}/></Link>)}</div></section><section id="zonas" className="market-section"><div className="section-head"><div><span className="kicker">ZONAS</span><h2>Explorá por ubicación</h2></div></div><div className="zone-grid">{Object.entries(zones).map(([slug,name])=><Link className="zone-card" href={'/categoria/kioscos?zona='+slug} key={slug}><MapPin size={18}/><div><strong>{name}</strong><span>{commerces.filter(c=>c.zone===slug).length} comercios demo</span></div><Navigation size={15}/></Link>)}</div></section><section id="comercios" className="market-section"><div className="section-head"><div><span className="kicker">DESTACADOS</span><h2>Comercios</h2></div><span className="section-note">Abrí una ficha directa</span></div><div className="commerce-grid">{commerces.map(c=><Link className="commerce-card" href={'/comercio/'+c.slug} key={c.slug}><div className={'commerce-thumb '+c.accent}>{c.name.split(' ').map(x=>x[0]).slice(0,2).join('')}</div><div><strong>{c.name}</strong><span>{c.categoryLabel} · {c.zoneLabel}</span><small>{c.hours}</small></div><ArrowUpRight size={16}/></Link>)}</div></section></main><PublicFooter/></div>}